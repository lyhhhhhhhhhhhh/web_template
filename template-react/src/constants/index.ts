// 角色常量
export const ROLES = {
    ADMIN: 'admin',
    USER: 'user'
};

// 本地存储键
export const STORAGE_KEYS = {
    TOKEN: 'token',
    REMEMBER_ME: 'remember_me',
    USER_INFO: 'user_info'
};

// API响应码
export const API_CODE = {
    SUCCESS: 0
};

// 路由路径
export const ROUTES = {
    LOGIN: '/login',
    REGISTER: '/register',
    HOME: '/home',
    USERS: '/users',
    PERMISSIONS: '/permissions',
    PROFILE: '/profile'
};